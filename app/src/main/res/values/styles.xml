<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/SoundllyColor</item>
        <item name="colorPrimaryDark">@color/SoundllyColor</item>
        <item name="colorAccent">@color/SoundllyColor</item>
    </style>

</resources>
